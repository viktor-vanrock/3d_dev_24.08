"""Диагностика печати по фото — Фаза 1 (MF-360): каталог дефектов, приём фото, контракт API.

Домен дефектов — `catalog.py` (`defects.json`). Приём и предобработка фото —
`photos.py`. HTTP-контракт — `schemas.py`, эндпоинты — `giga.main`.
Реальный анализ фото через GigaChat Vision — MF-361/362, ещё не реализован;
пока эндпоинт матчит по тексту описания (`heuristics.py`), см. её докстринг.
"""

from __future__ import annotations
