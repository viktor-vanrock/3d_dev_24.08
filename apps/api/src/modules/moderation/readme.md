# Moderation module

The standalone moderation route delegates user mutation to the profile owner through its public port.
The module owns the shared `reports` table; report commands move here as make/model routes migrate.
